import logo from "../trifecta.svg";
import React from "react";

const Icon = () => {
   return (
      <div className="Logo">
         <img src={logo} className="App-logo" alt="" />
      </div>
   );
};

export default Icon;
